{
  "name": "real-time-quizz",
  "private": true,
  "workspaces": [
    "frontend",
    "backend",
    "shared"
  ],
  "scripts": {
    "dev": "concurrently \"npm run dev --workspace=frontend\" \"npm run start:dev --workspace=backend\"",
    "install:all": "npm install && npm run prisma:generate --workspace=backend && npx prisma migrate deploy --workspace=backend"
  },
  "devDependencies": {
    "concurrently": "^8.2.0"
  }
}